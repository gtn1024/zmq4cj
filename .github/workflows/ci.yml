name: ci

on: [push, pull_request]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install cmake (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y cmake
      - uses: Zxilly/setup-cangjie@v2
        with:
          channel: sts
          version: 1.1.0
      - name: build
        shell: bash
        run: cjpm build
      - name: debug list vendor/build
        shell: bash
        run: find vendor/build -type f -name '*.a' -o -name '*.lib' 2>/dev/null || ls -R vendor/build/ 2>/dev/null || echo "no vendor/build"
      - name: test
        shell: bash
        run: cjpm test
