## 1. 实现

- [x] 1.1 在 `src/zmq_types.cj` 的 `SocketType` 枚举中新增 XPUB(9)、XSUB(10)、STREAM(11)
- [x] 1.2 新增测试：XPUB/XSUB 订阅转发（XSUB → XPUB → SUB 拓扑）
- [x] 1.3 新增测试：STREAM socket 可创建

## 2. 文档

- [x] 2.1 更新 `README.md` SocketType 表格
- [x] 2.2 更新 `README-zh.md` SocketType 表格（与英文对齐）
